const contacts = (state, { type, payload }) => {
  switch (type) {
    case "Get_Contacts":
      return state;
      break;

    default:
      return state;
      break;
  }
};

export default contacts;
